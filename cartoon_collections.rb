def greet_characters(array)
  array.each do |element|
  puts "Hello #{element}!"
  end
end
# Use `each` to enumerate over the provided arra

def list_dwarves(array) i
  
  #
  # Print a numbered list of each elementend
